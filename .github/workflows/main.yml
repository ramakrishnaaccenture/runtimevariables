# .github/workflows/main.yml

name: Main Workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run Action 1
      uses: .github\workflows\action1.yml # Reference the first action file

    - name: Run Action 2
      uses: .github\workflows\action2.yml  # Reference the second action file
